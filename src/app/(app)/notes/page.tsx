import { NotesWorkspace } from "@/modules/notes/notes-workspace";

export default function Page() {
  return (
    <div className="flex min-h-0 w-full flex-1 flex-col">
      <NotesWorkspace />
    </div>
  );
}
